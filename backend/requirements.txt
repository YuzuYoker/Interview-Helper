# Web 框架
fastapi>=0.140,<0.142
uvicorn[standard]>=0.35
python-multipart>=0.0.20
pydantic>=2.11
pydantic-settings>=2.14,<3

# LangChain 1.x（官方 Agent 框架：langchain.agents.create_agent + middleware）
langchain>=1.3,<2
langchain-openai>=1.4,<2        # >=1.1.14 已修复 SSRF 漏洞
langchain-qdrant>=1.1,<2
langchain-text-splitters>=0.3.7
langchain-huggingface>=1.0,<2   # 备用，主路径用自写 embedding
langgraph>=1.2,<2               # Agent 图引擎（create_agent 编译图）
langgraph-checkpoint>=4.1,<5    # 检查点（短期记忆/会话状态）
langgraph-prebuilt>=1.1,<2      # 预置节点（ToolNode 等）

# 向量库（local 模式无需额外依赖）
qdrant-client>=1.18,<2

# 本地嵌入模型（torch 已单独安装 CUDA 版）
sentence-transformers>=5.6,<6
transformers>=4.49

# 文档处理
pypdf>=5.0
python-docx>=1.2
openpyxl>=3.1

# 混合检索（BM25 + jieba 中文分词）
rank-bm25==0.2.2
jieba==0.42.1

# 缓存
redis>=8.1,<9

# 联网搜索（MCP 客户端 + fetch server，提供 fetch 工具；搜索=抓 Bing 结果页解析）
mcp>=1.1.3,<2  # fetch server 2026.7.10 用旧命名 McpError，2.x 不兼容
mcp-server-fetch
curl_cffi>=0.16  # 模拟 Chrome TLS 指纹（JA3），绕过容器环境被 Bing 判 bot 返回 CAPTCHA
